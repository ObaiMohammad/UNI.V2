spring.datasource.url=jdbc:postgresql://localhost:5432/padova
spring.datasource.username=postgres
spring.datasource.password=0808
spring.jpa.show-sql=true

## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
#Hibernate will automatically create database table, so you only need to manually create the database and configure
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect

server.error.include-message=always
server.error.include-binding-errors=always